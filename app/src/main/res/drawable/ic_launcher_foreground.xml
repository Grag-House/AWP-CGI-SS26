<?xml version="1.0" encoding="utf-8"?>
<!-- Adaptive icons only render the inner ~66dp safe zone of the 108dp canvas unclipped — ic_temi_robot
     fills almost its whole viewport, so used directly it gets cropped by the launcher's icon mask. This
     inset shrinks it down so it fits inside that safe zone instead, without touching ic_temi_robot itself
     (it's also used at its original size elsewhere, e.g. DashboardContent). -->
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    android:drawable="@drawable/ic_temi_robot"
    android:insetLeft="20%"
    android:insetTop="20%"
    android:insetRight="20%"
    android:insetBottom="20%" />
